# Copy this file to .env and adjust as needed.
# If MongoDB is not reachable, the backend automatically falls back to
# an in-memory sample dataset, so this is optional for local testing.

MONGO_URI=mongodb://localhost:27017
DB_NAME=pricesmart_ai
JWT_SECRET=change-this-secret-in-production
